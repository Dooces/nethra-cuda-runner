<!-- DONT USE THIS FUCKING FILE ITS FUCKING OLD LOOK AT IT FOR REFERENCE ONLY YOU FUCKING CUNT -->
# Nethra native whole-support freeze — 2026-09-23

Base lineage: `nethra-current-baseline` at
`e98f4fb9a7d5c493bb6dc53c2d4fcb60dd56c12b`.

This freeze integrates previously settled Nethra learning semantics into the one-file core.

## Live learning

1. The only persistent participant type is ordinary `Nethra`.
2. Current independently sourced Nethra are kept as source provenance.
3. Complete recursive closure is refound from that source support before construction.
4. Existing prior-interval field flow supplies prospective prediction.
5. The current consequence is measured by identical current-interval replay from the same
   pre-outcome state:
   - actual field with the current external source;
   - counterfactual field with zero new external source;
   - positive manifestation target
     `M_i = C * max(0, a_actual_i - a_zero_i)`.
   This allows internally manifested recursive Nethra to be real consequences without declaring
   them independently sourced.
6. Per-incidence residual:
   `epsilon_i = M_i - P_i`.
7. Relation-local tension:
   `T_R = sum_i p_Ri * epsilon_i`.
8. Outgoing incidence evidence:
   `Delta e_Ri = eta_out * p_Ri * epsilon_i`.
9. Supplying incidences receive `eta_in * T_R` in proportion to their actual positive incoming
   charge to the relation.
10. The same per-Nethra `epsilon_i` updates frozen V61 `rho_i` before any new topology is
    admitted; newly relevant supplier pairs therefore begin with no fabricated residual history.
11. Evidence is continuous and clamped only at zero; native default has `g(0)=0`.
12. Admission is permissive and grounded only by positive unresolved residual on independently
    sourced current support.
13. A newly admitted hypothesis is an ordinary weak Nethra over the whole recursively closed
    before/current support. No proper-subset candidate generation exists.
14. Existing structure is checked before minting. Refound structure that already accounts for the
    event is reused.
15. If the reused relation appears inside the recursive whole-support description, that
    self-containing description is tautological: it may account/subtract, but is never registered
    as fresh support for itself.
16. Unsupported hypotheses are retained; incidence evidence may decay to zero and make them
    field-inert. No deletion/forgetting rule is introduced.

## Removed from live core

- `_consider_completed_interval_provisional()`;
- history/support/outcome probability counters;
- probability/conditional/baseline construction authority;
- `_mint_history()` from the retired learner;
<!-- DONT USE THIS FUCKING FILE ITS FUCKING OLD LOOK AT IT FOR REFERENCE ONLY YOU FUCKING CUNT -->
- candidate clouds, subset scanners, winner rules, external consequence selectors.

## Preserved

- finite-interval RK4 F61 field mechanics;
- exact external-source provenance;
- exact activation delta;
- per-Nethra interval integral `A_i`;
- exact reconstructible prior incidence charge `Q_ij=g_ij(A_i-A_j)`;
- recursive closure/refinding and ambiguity;
- empty-projection closure correction;
- direct-self-route rejection and legal longer cycles;
- per-route/member persistent evidence;
- subtraction/accounting before construction.

## Maintenance

`nethra.py` requires `NETHRA_MISTAKE_LEDGER.md` to be read before interpretation or modification.
Every newly discovered assistant/developer mistake, regression, contamination, false claim, or
shortcut must be appended to that ledger before a semantic change.
