name: AAPL current-event continuation comparison

on:
  push:
    branches:
      - nethra-aapl-current-event-fix
    paths:
      - 'aapl_residual_recursive_native.py'
      - 'aapl_continuation_baselines.py'
      - '.github/workflows/aapl-current-event-compare.yml'
  workflow_dispatch:

jobs:
  compare:
    runs-on: self-hosted
    timeout-minutes: 4
    env:
      PYTHONHASHSEED: '0'
      NETHRA_COMPARE_TRAIN: '1200'
      NETHRA_COMPARE_ONLINE: '5000'
    steps:
      - uses: actions/checkout@v4
      - name: Compare price-sensitive continuation after current-event fix
        shell: bash
        run: |
          set -euxo pipefail
          PYTHONPATH=. /usr/bin/time -f 'CURRENTEVENT elapsed=%e maxrss_kb=%M'             /usr/bin/python3 aapl_continuation_baselines.py
