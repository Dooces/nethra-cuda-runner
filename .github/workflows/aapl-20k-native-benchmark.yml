name: AAPL 20k native benchmark

on:
  push:
    branches:
      - nethra-aapl-native-recursive-depth
    paths:
      - '.github/workflows/aapl-20k-native-benchmark.yml'
      - 'aapl_residual_recursive_native.py'
  workflow_dispatch:

jobs:
  benchmark:
    runs-on: self-hosted
    timeout-minutes: 6
    env:
      NETHRA_STOCK_PRICES: '20000'
      NETHRA_STOCK_REPLAYS: '1'
      NETHRA_STOCK_ABLATION: '500'
    steps:
      - uses: actions/checkout@v4
      - name: One full 20k-price pass
        shell: bash
        run: |
          set -euxo pipefail
          PYTHONPATH=. /usr/bin/time -v /usr/bin/python3 aapl_residual_recursive_native.py

  benchmark_cloud:
    runs-on: ubuntu-latest
    timeout-minutes: 6
    env:
      NETHRA_STOCK_PRICES: '20000'
      NETHRA_STOCK_REPLAYS: '1'
      NETHRA_STOCK_ABLATION: '500'
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Dependencies
        run: python -m pip install --quiet numpy numba
      - name: One full 20k-price pass
        shell: bash
        run: |
          set -euxo pipefail
          PYTHONPATH=. /usr/bin/time -f 'elapsed=%e maxrss_kb=%M' python aapl_residual_recursive_native.py
