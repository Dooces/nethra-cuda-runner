name: Nethra route flux deterministic quick
on:
  push:
    branches: [nethra-self-feed-audit]
    paths:
      - 'nethra_route_flux_deterministic_quick.py'
      - '.github/workflows/nethra-route-flux-deterministic-quick.yml'
  workflow_dispatch:
jobs:
  audit:
    runs-on: ubuntu-latest
    timeout-minutes: 3
    steps:
      - uses: actions/checkout@v4
      - shell: bash
        run: PYTHONPATH=. /usr/bin/python3 nethra_route_flux_deterministic_quick.py
