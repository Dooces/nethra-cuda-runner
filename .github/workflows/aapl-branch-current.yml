name: AAPL branch-current confidence

on:
  push:
    branches:
      - nethra-aapl-confidence-calibration
    paths:
      - 'aapl_single_target_context.py'
      - '.github/workflows/aapl-branch-current.yml'
  workflow_dispatch:

jobs:
  audit:
    runs-on: self-hosted
    timeout-minutes: 3
    env:
      PYTHONHASHSEED: '0'
      NETHRA_MULTI_CACHE: '/tmp/nethra_multisymbol_exact.json'
      NETHRA_SYMBOLS: 'AAPL,MSFT,NVDA,TSLA,JPM,XOM'
      NETHRA_SINGLE_TRAIN: '800'
      NETHRA_SINGLE_ONLINE: '1000'
    steps:
      - uses: actions/checkout@v4
      - name: Actual prospective branch-current audit
        shell: bash
        run: |
          set -euxo pipefail
          PYTHONPATH=. /usr/bin/time -f 'BRANCH elapsed=%e maxrss_kb=%M'             /usr/bin/python3 aapl_single_target_context.py
