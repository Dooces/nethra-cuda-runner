name: AAPL residual recursive native

on:
  push:
    branches:
      - nethra-aapl-native-recursive-depth
    paths:
      - 'aapl_residual_recursive_native.py'
      - '.github/workflows/aapl-residual-recursive-native.yml'
  workflow_dispatch:

jobs:
  sanity_cloud:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    env:
      NETHRA_STOCK_PRICES: '2000'
      NETHRA_STOCK_REPLAYS: '10'
      NETHRA_STOCK_ABLATION: '500'
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Dependencies
        run: python -m pip install --quiet numpy numba
      - name: Short execution sanity
        shell: bash
        run: |
          set -euxo pipefail
          PYTHONPATH=. /usr/bin/time -f 'elapsed=%e maxrss_kb=%M' python aapl_residual_recursive_native.py

  sanity:
    runs-on: self-hosted
    timeout-minutes: 3
    env:
      NETHRA_STOCK_PRICES: '2000'
      NETHRA_STOCK_REPLAYS: '2'
      NETHRA_STOCK_ABLATION: '500'
    steps:
      - uses: actions/checkout@v4
      - name: Short execution sanity
        shell: bash
        run: |
          set -euxo pipefail
          PYTHONPATH=. /usr/bin/time -f 'elapsed=%e maxrss_kb=%M' /usr/bin/python3 aapl_residual_recursive_native.py
