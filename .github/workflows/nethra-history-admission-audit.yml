name: Nethra history admission audit
on:
  push:
    branches: [nethra-self-feed-audit]
    paths:
      - 'nethra_history_admission_audit.py'
      - '.github/workflows/nethra-history-admission-audit.yml'
  workflow_dispatch:
jobs:
  audit:
    runs-on: self-hosted
    timeout-minutes: 3
    steps:
      - uses: actions/checkout@v4
      - shell: bash
        run: PYTHONPATH=. /usr/bin/python3 nethra_history_admission_audit.py
