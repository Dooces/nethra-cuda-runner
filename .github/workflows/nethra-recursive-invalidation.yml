name: Nethra recursive invalidation

on:
  push:
    branches:
      - nethra-recursive-invalidation-audit
    paths:
      - 'recursive_invalidation_probe.py'
      - 'recursive_invalidation_fast_probe.py'
      - '.github/workflows/nethra-recursive-invalidation.yml'
  workflow_dispatch:

jobs:
  invalidation:
    runs-on: self-hosted
    timeout-minutes: 2
    steps:
      - uses: actions/checkout@v4
      - name: Recursive invalidation
        shell: bash
        run: |
          set -euxo pipefail
          echo '=== semantic invalidation ==='
          PYTHONPATH=. /usr/bin/time -f 'semantic_elapsed=%e maxrss_kb=%M' /usr/bin/python3 recursive_invalidation_probe.py
          echo '=== indexed invalidation ==='
          PYTHONPATH=. /usr/bin/time -f 'indexed_elapsed=%e maxrss_kb=%M' /usr/bin/python3 recursive_invalidation_fast_probe.py
