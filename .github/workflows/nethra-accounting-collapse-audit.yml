name: Nethra accounting collapse audit
on:
  push:
    branches: [nethra-self-feed-audit]
    paths:
      - 'nethra_accounting_collapse_audit.py'
      - '.github/workflows/nethra-accounting-collapse-audit.yml'
  workflow_dispatch:
jobs:
  audit:
    runs-on: self-hosted
    timeout-minutes: 3
    steps:
      - uses: actions/checkout@v4
      - shell: bash
        run: PYTHONPATH=. /usr/bin/python3 nethra_accounting_collapse_audit.py
